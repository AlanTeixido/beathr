services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: beathr-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.production

networks:
  default:
    name: beathr-network
